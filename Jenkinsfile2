pipeline {
    agent any
    tools {
        maven 'M2_HOME'
    }
    stages {
        stage('maven command'){
            steps{
                sh 'mvn clean test compile install package'
            }
        }
    }
}