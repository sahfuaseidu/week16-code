pipeline {
    agent any
    tools {
        maven 'M2_HOME'
    }
    stage{
        stage('maven command'){
            steps{
                sh 'mvn clean compile install package'
            }
        }
    }
}